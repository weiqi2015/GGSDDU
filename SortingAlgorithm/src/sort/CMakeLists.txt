aux_source_directory(. SORT_FILE_LIST)
set(LIBRARY_OUTPUT_PATH ${PROJECT_SOURCE_DIR}/lib)
message(STATUS "SORT_FILE_LIST:${SORT_FILE_LIST}")
add_library(sort STATIC ${SORT_FILE_LIST})
message(STATUS "LIBRARY_OUTPUT_PATH:${PROJECT_SOURCE_DIR}/lib")
